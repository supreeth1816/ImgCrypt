
enum AppRunningState {
  INTEGRATION_TEST,
  UNIT_TEST,
  DEVELOPMENT,
  PRODUCTION,
}

enum PlatformState {
  IPAD,
  IPHONE,
  GENERIC,
}


enum DecodeResultState {
  SUCCESS,
  ERROR,
}


enum LoadingState {
  LOADING,
  ERROR,
  PENDING,
  SUCCESS,
}
